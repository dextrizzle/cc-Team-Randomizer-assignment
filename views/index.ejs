<%- include('partials/header') %>

<h1 style="margin-bottom:100px;">teamRandomizer</h1>

<%# putting code inside the tags 👇 will execute it, but not print it %>
<%# we have access to params as a variable because you passed
and object of the shape
{params: {fullName: 'dasdas', email: 'dasdas', names: 'dasdas'}}
in the res.post('/context', function ...) router
%>


  <form method="post">

    <div>
      <label for="names">Names: </label>
      <textarea id="names" name="names" placeholder="john, doe, etc..."></textarea>
    </div>

    <div>
      <label for="names">Members per team: </label>
      <input type="number" name="quantity" min="1">

    </div>

    <div>
      <input id="submit" name="submit" type="submit"/>
    </div>
  </form>

<% if (pick) { %>
  <h1> Teams: </h1>
  <% for(let i=0;i<pick.length;i++){ %>
  <p>Team <%= i+1 %>:
  <% for(let j=0;j<pick[i].length;j++) {
    if(j==pick[i].length-1 ){%><%=pick[i][j]%><% } else { %><%= pick[i][j] %>, <%}%><% } %></p><% } %><% } %>

<%- include('partials/footer') %>
